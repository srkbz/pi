version: '3'

output: prefixed

tasks:
  package:
    cmds:
      - apt-get install samba
    status:
      - '[ -f /usr/sbin/smbd ]'

  configure:
    deps: [package]
    cmds:
      - cp ./smb.conf /etc/samba/smb.conf
      - systemctl restart smbd.service
    sources:
      - ./smb.conf
